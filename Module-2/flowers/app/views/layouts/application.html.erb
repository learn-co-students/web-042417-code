<!DOCTYPE html>
<html>
  <head>
    <title>Flowers</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <a class="navbar-brand" href="/">Lotus</a>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="/flowers/new">New
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Link</a>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" href="#">Disabled</a>
          </li>
        </ul>
        <% if current_user %>
        <p>
          hello,
          <%= current_user.email %>
          <%= link_to 'Logout', logout_path, method: :delete, data: {confirm: 'Are you sure?'} %>
        </p>
      <% else %>
        <%= form_tag  sessions_path, class: "form-inline my-2 my-lg-0"  do %>
        <%= text_field_tag :email, nil, placeholder: "example@aol.com", class: "form-control mr-sm-2"  %>
        <%= password_field_tag :password,nil,  placeholder: "dragons", class: "form-control mr-sm-2"  %>
        <%= submit_tag "log in", class: "btn btn-outline-success my-2 my-sm-0"  %>
        <% end %>
        <% end %>

      </div>
    </nav>
    <% flash.each do |name, msg| %>
    <% if name == "error" %>
    <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
    <%= content_tag :div, msg, class: "alert alert-danger", role: "alert" %>
  <% else %>
    <%= content_tag :div, msg, class: "alert alert-success", role: "alert" %>
    <% end %>
    <% end %>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
</div>
</body>
</html>
